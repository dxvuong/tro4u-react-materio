/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com"],
    unoptimized: true,
  },
  // output: 'export',
  // trailingSlash: true,
};

module.exports = nextConfig;
